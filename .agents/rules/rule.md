---
trigger: always_on
---

- **publicリポジトリとして問題ないように保つため、秘匿情報はコミットしない**
- 1まとまりの作業完了する度に、自動的にmainブランチに直接コミット・PUSH
- DartのCLIツールの作成やスキル名には `dart_cli_creator` ではなく `dart-cli-creator` というケバブケースの名前を使用するように統一してください。
